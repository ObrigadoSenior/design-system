export type ThemeMetricsProps = {
  width: {
    min: string;
    max: string;
  };
  sizes: {
    sizeNone: string;
    sizeXs: string;
    sizeS: string;
    sizeM: string;
    sizeL: string;
    sizeXl: string;
  };
};
