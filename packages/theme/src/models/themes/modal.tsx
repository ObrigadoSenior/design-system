export type ThemeModalProps = {
  backgroundColor: string;
  borderColor: string;
  color: string;
  borderRadius: string;
  padding: string;
  shadow: string;
  transitionSpeed: string;
  overlayColor: string;
};
