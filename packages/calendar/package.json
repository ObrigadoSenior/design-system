{
  "name": "@obrigado-senior/calendar",
  "version": "0.0.3",
  "description": "Obrigado Senior Design System - Calendar",
  "author": "Johan Sjöberg",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "config": {
    "unsafe-perm": true
  },
  "scripts": {
    "build": "rollup -c",
    "start": "rollup -c -w",
    "clean": "rm -rf dist",
    "test": "jest",
    "test-watch": "jest --watch",
    "prepare": "yarn run build",
    "postpublish": "yarn clean",
    "lint": "eslint --ignore-path ../../.gitignore --fix"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.11.5",
    "jest": "^26.4.2",
    "rollup": "^2.27.1",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@obrigado-senior/theme": "0.0.3"
  },
  "peerDependencies": {
    "react": ">=16.8.6",
    "react-dom": ">=16.8.6"
  },
  "jest": {
    "moduleNameMapper": {
      "\\.(css|less|sass|scss)$": "<rootDir>/__mocks__/styleMock.js",
      "\\.(gif|ttf|eot|svg)$": "<rootDir>/__mocks__/fileMock.js"
    },
    "setupFilesAfterEnv": [
      "./setupTests.ts"
    ]
  },
  "gitHead": "1708ccdbfccc75759846fbdbe65d22fe0826b8ad"
}
