import { DatePicker } from './';
import { StoriesDivider } from '../../../../../.storybook/storiesTheme/StoriesDivider';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

<Meta title="Calendar/Datepicker" component={DatePicker} />

# Molecules

## Datepicker

> `Datepicker` for when user needs to input dates. Can be a horizontal "one month per page" calendar or a vertical basic "infinite scrolle" calendar.

# Preview

<Preview>
  <Story name="Datepicker">
    <StoriesDivider labels={['Standard', 'Basic']}>
      <DatePicker onChange={() => {}} locale="sv-SE" />
      <DatePicker onChange={() => {}} locale="sv-SE" basic={true} />
    </StoriesDivider>
  </Story>
</Preview>

# Props

<Props of={DatePicker} />
