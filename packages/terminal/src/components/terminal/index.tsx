import cx from 'classnames';
import React from 'react';
import { TerminalProps } from '../../types';
import styles from './Terminal.module.scss';

export const Terminal = ({}: TerminalProps): JSX.Element => {
  return <div></div>;
};
