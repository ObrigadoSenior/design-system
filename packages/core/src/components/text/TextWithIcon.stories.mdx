import { cookie } from '../../../../icons';
import { Text } from './';
import { StoriesDivider } from '../../../../../.storybook/storiesTheme/StoriesDivider';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

<Meta title="Core/Text/With Icon" component={Text} />

# Atoms

## Text with icon

> `Text with icon` as a core element. Has the function as building block for molecules or organisms.

# Preview

<Preview>
  <Story name="With Icon">
    <StoriesDivider labels={['Icon to the left', 'Icon to the right', 'Icon to left and right']}>
      <Text leftIcon={{ icon: cookie, size: 'l' }} size="s">
        Text with icon to the left
      </Text>
      <Text rightIcon={{ icon: cookie, size: 'l' }} size="s">
        Text with icon to the right
      </Text>
      <Text leftIcon={{ icon: cookie, size: 'l' }} rightIcon={{ icon: cookie, size: 'l' }} size="s">
        Text with icon to the right
      </Text>
    </StoriesDivider>
  </Story>
</Preview>

# Props

<Props of={Text} />
