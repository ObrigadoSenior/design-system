import { Price } from './';
import { StoriesDivider } from '../../../../../.storybook/storiesTheme/StoriesDivider';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

<Meta title="Core/Price" component={Price} />

# Atoms

## Price

> `Price` as a core element. Has the function as building block for molecules or organisms.

# Preview

<Preview>
  <Story name="Price">
    <StoriesDivider labels={['Standard', 'Same size', 'No italic']}>
      <Price left={{ value: 'From' }} middle={{ value: '$999' }} right={{ value: '/Person' }} />
      <Price
        left={{ value: 'From', props: { size: 'l' } }}
        middle={{ value: '$999', props: { size: 'l' } }}
        right={{ value: '/Person', props: { size: 'l' } }}
      />
      <Price
        left={{ value: 'From', props: { fontStyle: 'normal' } }}
        middle={{ value: '$999', props: { fontStyle: 'normal' } }}
        right={{ value: '/Person', props: { fontStyle: 'normal' } }}
      />
    </StoriesDivider>
  </Story>
</Preview>

# Props

<Props of={Price} />
