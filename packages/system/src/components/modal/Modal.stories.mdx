import { Modal } from './';
import { StoriesDivider } from '../../../../../.storybook/storiesTheme/StoriesDivider';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

<Meta title="System/Modal" component={Modal} />

# Atoms

## Modal

> `Modal` as a core element. Has the function as container for molecules or organisms.

# Preview

<Preview>
  <Story name="Modal">
    <StoriesDivider labels={['Modal']}>
      <Modal onClick={() => console.log('clicked')} open={false}>
        <div>Manipulated</div>
      </Modal>
    </StoriesDivider>
  </Story>
</Preview>

# Props

<Props of={Modal} />
