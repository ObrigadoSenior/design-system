import { Radio } from './';
import { StoriesDivider } from '../../../../../.storybook/storiesTheme/StoriesDivider';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

<Meta title="Forms/Radio" component={Radio} />

# Atoms

## Radio

> `Radio` as a core element. Has the function as building block for molecules or organisms.

# Preview

<Preview>
  <Story name="Radio">
    <StoriesDivider labels={['Unchecked', 'Checked', 'Unchecked - disabled', 'Checked - disabled']}>
      <Radio label="One way" onChange={() => console.log()} />
      <Radio checked label="One way" onChange={() => console.log()} />
      <Radio disabled label="One way" onChange={() => console.log()} />
      <Radio checked disabled label="One way" onChange={() => console.log()} />
    </StoriesDivider>
  </Story>
</Preview>

# Props

<Props of={Radio} />
