import { StoriesDivider } from '../../../../../.storybook/storiesTheme/StoriesDivider';
import { Dropdown } from './';
import { IDropdownOption } from '../../models/components/dropdown';
import { arrowRight, placeholder } from '../../../../icons';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

<Meta title="Forms/Dropdown" component={Dropdown} />

# Atoms

## Dropdown

> `Dropdown` as a core element. Has the function as building block for molecules or organisms.

# Preview

<Preview>
  <Story name="Dropdown">
    <StoriesDivider labels={['Standard', 'Default value', 'Disabled']}>
      <Dropdown
        title="Input"
        placeholder="Placeholder"
        onChange={() => console.log()}
        onOptionClick={() => console.log()}
        options={[
          {
            title: 'Option',
            value: 'option0',
            id: 'opt0',
            data: '0',
          },
        ]}
      />
      <Dropdown
        defaultValue="Deafult value"
        title="Input"
        placeholder="Placeholder"
        onChange={() => console.log()}
        onOptionClick={() => console.log()}
        options={[
          {
            title: 'Option',
            value: 'option0',
            id: 'opt0',
            data: '0',
          },
        ]}
      />
      <Dropdown
        disabled
        title="Input"
        placeholder="Placeholder"
        onChange={() => console.log()}
        onOptionClick={() => console.log()}
        options={[
          {
            title: 'Option',
            value: 'option0',
            id: 'opt0',
            data: '0',
          },
        ]}
      />
    </StoriesDivider>
  </Story>
</Preview>

# Props

<Props of={Dropdown} />
